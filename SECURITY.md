# Security Policy

## Supported version

DooDChat 目前是小型单版本项目，只维护默认分支中的最新版本。旧压缩包、旧提交和第三方修改版本不提供安全更新保证。

## 报告安全问题

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。若仓库尚未启用私密漏洞报告，请先通过仓库所有者的 GitHub 主页联系维护者，不要直接创建包含利用细节的公开 Issue。

报告时请提供：

- 受影响版本或提交
- 最小复现步骤
- 实际影响
- 建议的修复方向（可选）

请勿发送真实房间密码、Cloudflare Tunnel 地址、访问令牌、聊天内容、个人 IP、证书或其他隐私数据；使用虚构测试值替代。

## 安全边界

DooDChat 面向少量互相信任的朋友临时使用，不是公共运营平台。即使项目包含认证隔离、协议校验、限流、上传和音频代理保护，也不建议无管理地暴露到公网。

- WebRTC 语音由浏览器在参与者之间建立连接，并使用 Google STUN 服务；参与者及 STUN 服务可能获知网络地址。不要把它当作隐藏 IP 的匿名语音服务。
- 音乐封面由浏览器直接从网易云加载，网易云可能收到访问请求；歌单和歌词由本机服务向网易云查询。
- 图片上传接口需要已认证用户的令牌，但生成的临时图片 URL 不再要求房间密码；任何获知 URL 的人都可能在图片清理前读取图片。请不要上传敏感图片，也不要公开转发图片链接。
- 浏览器界面对 JPEG、PNG、WebP 重新编码以减少原图元数据；GIF 为保留动画而原样上传。服务端不保证剥离 EXIF、定位信息或其他元数据，其他客户端也可直接上传原始图片。
- DooDChat 的自动清理仅覆盖带有效归属标记的服务器临时目录和内存状态，不覆盖浏览器历史、系统备份、代理/穿透日志、截图或接收方保存的内容。

建立任何公网入口时：

1. 必须设置非空且足够强的 `ROOM_PASSWORD`。
2. 必须使用 HTTPS/WSS。
3. 不要直接无保护地开放路由器3000端口。
4. 使用结束后停止公网 Tunnel，并安全关闭 DooDChat。
5. 不要提交 `.env`、证书、Tunnel 凭据、运行缓存或聊天媒体。

更多实现边界见 [`README.md`](./README.md) 的「安全说明」以及 [`server/security.js`](./server/security.js)、[`server/config.js`](./server/config.js)。
